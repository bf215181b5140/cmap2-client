export const enum WindowState {
    HIDE,
    MINIMIZE,
    CLOSE,
}

export const enum SocketConnectionState {
    CONNECTING,
    CONNECTED,
    AUTHENTICATION_FAILED,
    DISCONNECTED,
    ERROR,
}
