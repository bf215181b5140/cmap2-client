export enum WindowSize {
    Big = 'Big',
    Medium = 'Medium',
    Small = 'Small'
}

export enum WindowState {
    Open = 'Open',
    Minimize = 'Minimize',
    Tray = 'Tray',
    Exit = 'Exit',
}
